package org.firstinspires.ftc.teamcode;

public class DistanceUnit {
    public double COUNTS_PER_UNIT = 0;
    public DistanceUnit(double COUNTS_PER_UNIT) {

        this.COUNTS_PER_UNIT = COUNTS_PER_UNIT;
    }
}
